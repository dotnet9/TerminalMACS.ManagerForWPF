<UserControl x:Class="Dotnet9Games.Views.BallGame"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border>
        <Grid>
            <Rectangle Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFD8E6F3" Offset="0" />
                        <GradientStop Color="#FFB0D1F0" Offset="1" />
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>
            <Canvas x:Name="CanvasPlayground">
            </Canvas>
        </Grid>
    </Border>
</UserControl>